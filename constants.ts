import { DressType, MaterialColor, AccessoryType } from './types';

export const DRESS_OPTIONS = Object.values(DressType);
export const COLOR_OPTIONS = Object.values(MaterialColor);
export const ACCESSORY_OPTIONS = Object.values(AccessoryType);

export const SAMPLE_PROMPTS = [
  "A futuristic fashion editorial shot.",
  "Earthy, sustainable luxury aesthetic.",
  "High-end runway photography."
];
